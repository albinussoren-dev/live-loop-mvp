services:
  api:
    build: ./backend
    ports:
      - "8000:8000"
    env_file: .env
    environment:
      DATABASE_URL: sqlite:////app/data/looplive.db
    volumes:
      - looplive-data:/app/data
    restart: unless-stopped
    init: true
    stop_grace_period: 30s
volumes:
  looplive-data:
